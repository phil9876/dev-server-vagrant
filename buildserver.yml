---

- hosts: all
  roles:
    - java
    - postgresql
    - sonarqube
    - jenkins
    - { role: nexus, nexus_installation_dir: '/opt'}
    - gitlab
    # Needs checking - gocd_agent
    # Needs checking - gocd_server
    # Needs checking  - teamcity
